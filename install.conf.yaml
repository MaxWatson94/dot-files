- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.vim: vim/
    ~/.vimrc: vim/vimrc
    ~/.git: git/


- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - vim +PluginInstall +qall
