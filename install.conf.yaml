- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim: vim/
    ~/.vimrc: vim/vimrc
    ~/.git: git/

- create:
    - ~/downloads
    - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]
